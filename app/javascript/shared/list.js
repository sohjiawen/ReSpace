const furnitureButton = document.querySelector(".fa-couch")

furnitureButton.addEventListener('touchstart', handleInteraction)

const handleInteraction = () => {
    furnitureButton.insertAdjacentHTML()
}